# Terraform state — never commit these
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl
*.tfplan

# SSH keys — never commit these
id_ed25519
id_ed25519.pub
*.pem

# Local overrides
*.auto.tfvars
override.tf
