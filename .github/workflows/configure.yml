# ⚠️  This workflow has been superseded.
# Configuration (Ansible K8s bootstrap) is now the 'configure' job inside infra.yml.
# Kept here only for historical reference — it will never trigger automatically.

name: Configure K8s Nodes (merged into infra.yml)

on:
  workflow_dispatch:   # manual only — never triggered automatically

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "K8s cluster configuration is handled by the 'configure' job in infra.yml"
